services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    volumes:
      - ./data:/data
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE=http://backend:8001
    depends_on:
      - backend